import { TodomisnusculasDirective } from './todomisnusculas.directive';

describe('TodomisnusculasDirective', () => {
  it('should create an instance', () => {
    const directive = new TodomisnusculasDirective();
    expect(directive).toBeTruthy();
  });
});
