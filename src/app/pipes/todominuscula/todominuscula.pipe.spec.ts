import { TodominusculaPipe } from './todominuscula.pipe';

describe('TodominusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new TodominusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
